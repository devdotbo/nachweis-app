# Video shot list

Seven beats from the pitch, mapped to the screens and terminal windows in this repository. Rules (submission checklist, decided 2026-09-07): 2 to 4 minutes total, at least 720p, screen recording only, the builder speaks, no AI voice, no speedups, intro at most about 20 seconds. Every simulated or manual step is captioned. Where the proof is made is captioned. Only green work packages enter the video; nothing red is shown as if it worked.

Budget: 168 seconds of content, 12 seconds of slack for the closing card. Total under 3 minutes.

Windows to have open before recording: the app at `http://localhost:5173` (investor role), a second browser window with the issuer role, the phone screen (mirrored, or filmed by the laptop camera as a screen recording of the mirror), the bridge terminal (`RUST_LOG=info ./target/release/nachweis-bridge`), a terminal for `cast` and the swap script, and Etherscan Sepolia tabs for the registry and the transactions. How to bring each up: `docs/demo-runbook.md`.

| # | beat | max s | screen or terminal (this repo) | caption on screen | spoken |
|---|---|---|---|---|---|
| 0 | intro | 12 | title card: "Attestat" and the spoken line | "Your ID wallet should work where you invest." | the same line, then one sentence: an issuer accepts the state ID wallet once and the approval follows the customer's crypto wallet |
| 1 | the investor | 12 | app, Investor role: card 1 "Connect wallet" then card 3 "Eligibility" showing `not permitted`, card 4 "Two doors, one decision" with both doors closed | "identity evidence required" | she opens the fund app with her own wallet; the address is not permitted |
| 2 | the wallet | 28 | app card 2 "Present your ID": the QR; then the phone: the official German test wallet shows the request (given name, family name, over 18), she taps once; back on the app card 3 turns `presented, awaiting issuer` | "official test wallet, sample identity" and, on the phone shot, "names go to the issuer, not to the chain" | the state test wallet answers the issuer's registered request: given name, family name, over 18. The names go to the issuer's verifier, not to the chain |
| 3a | the binding | 10 | app card 2, the sign button; the wallet pop-up with `nachweis:session:<id>`; the bridge terminal line for `POST /sessions/:id/address-proof` | "signed by the crypto wallet, not by the ID wallet" | her crypto wallet signs the session; the presentation is bound to that address through the nonce |
| 3 | the issuer, the proof, the record | 38 | app, Issuer role: card 3 "Presentations" with the claims summary, the checks row, the Approve button (separate click); then the bridge terminal: the proof mode line and the timing line; then Etherscan: the `attestWithProof` transaction, Logs tab, `Attested` with bits, tier, expiry, policy id, status ref; then app Investor card 5 "What the chain sees" | "remaining checks: simulated"; "approval: manual"; "proof made by our server (SP1, Groth16), verified by the chain" or, if the Noir route is shown, "proof made on this laptop (Noir), verified fully on chain"; on Etherscan: "no name, no document" | the issuer matches the presentation to its record, the other checks are simulated, and approves. The chain does not trust our server: it verifies a zero-knowledge proof that a PID from the official test wallet, bound to this wallet, says over 18. The proof was generated by our verifier, not yet by the phone. What lands on chain: a few bits, a tier, an expiry, a policy id. No name |
| 4 | two doors | 28 | app Investor card 4: Subscribe, the NDF balance; then the Swap tile of the same card: "about 90.65 NDF", the three answers (registry, checker, adapter), Swap, `swap confirmed` with the hash and "Sent 100 mUSD, received 90.65 NDF" (`docs/swap.md`); Etherscan showing the swap through the permissioned Universal Router with the `PermissionedHooks` address in the trace | "one permission, two doors"; on the swap: "Uniswap v4 permissioned pool, allowlist checker reads the same record" | she subscribes to the demo fund token, then swaps in the Uniswap permissioned pool. No second presentation. One permission, two doors |
| 5 | the exhibit | 0 | cut: WP11 (Multipaz on-phone Longfellow proof) is optional and not green on 2026-09-07 | if it turns green: "test credential, open wallet, on-phone proof; the official wallet does not produce this proof today", 10 to 20 s taken from beats 3 and 4 | |
| 6 | the revoke | 22 | app Issuer card 3 Revoke (or card 2 "Revoke by address"); Issuer card 4 "Registry events" shows `Revoked`; Investor card 3 turns `revoked`, both doors closed; the Swap tile after clicking Swap again: `swap refused`, "Refused by PermissionedHooks.beforeSwap. Unauthorized(): this address is not SWAP_ALLOWED", the reverted transaction's hash, the decoded `WrappedError` in the details block; `cast send … subscribe()` reverting with `NotEligible` | "manual withdrawal"; on the reverts: "pool refuses", "fund refuses" | the issuer withdraws approval by hand. The fund and the pool both refuse the same address |
| 7 | limits, then stop | 18 | a plain text card with four lines, then the closing card | the four lines: "official German test wallet, sample identity"; "sanctions and other checks: simulated"; "proof made by our server (SP1) or on the laptop (Noir), captioned where shown; not yet on the phone"; "revocation: manual" | read the four lines, then the closing sentence |

Closing card text, on screen for the last 8 to 12 seconds:

    Attestat helps token issuers accept EUDI identity evidence and apply their approval to customers' linked crypto wallets, without putting identity documents on chain.

    no document on chain

Last spoken line is the closing card sentence.

## Notes for the recording

- Sentence for camera in beat 3 must match the route that shipped (`wiki/zk-plan.md`, "Sentences for camera"). With the SP1 route: "The chain does not trust our server, but the server did see the presentation." With the Noir desktop route: "the proof is generated on this laptop; the server never saw the presentation" only if the blind relay is in the loop, otherwise keep the server sentence.
- If Groth16 proving takes minutes on the day, cut once and caption the measured duration; no speedup.
- If the phone run (WP0) is on iOS with the birthdate fallback, the caption in beat 2 says which claim was shown.
- If the Sepolia run has not happened, beats 3, 4 and 6 are recorded against the local anvil or the Sepolia fork (`docs/demo-runbook.md`, sections 5 to 7) and captioned "local chain"; the Etherscan shots are replaced by `cast logs` and `decisionOf` in the terminal. If the app runs in mock mode, the caption says "mock".
- The issuer screen has no "simulated" label in the UI today; the caption carries it.
- No "first", no "only", no yield figures, no "real state-issued identity", no KYC outside the Uniswap form field name. Sponsor mentions: Uniswap only.
