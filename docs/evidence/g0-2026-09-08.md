# G0 run record, 2026-09-08 (sanitized)

Run directory (private, gitignored): docs/evidence/private/runs/20260908-205938. This file holds only shapes, sizes, hashes of public material and status codes. No names, no presentation, no pickup token, no witness.

## Setup

- Verifier: relay branch nachweis-relay at 7262a32, release build, relay mode, registrar-issued leaf (client_id x509_hash:VE3qp3vLVkU8JyVmXkjL7CSDVxVoTFdTv5fAEwmjKOI), issuer trust not enforced, status check offline, RESULT_TOKEN unset (relay path only).
- Tunnel: cloudflared 2026.8.2, quick tunnel https://cloud-scored-leeds-mpg.trycloudflare.com (gone after g0-down).
- Companion: nachweis-app main at 1c2161b plus the statement.ts fix below; bound address 0x70997970C51812dc3A010C7d01b50e0d17dc79C8 (anvil dev account, no funds involved).
- Phone: builder's iPhone with the official German EUDI test wallet (sandbox, sample identity). Wallet version string: not recorded (builder to add).

## What happened

1. 19:00:38 companion `request`: ephemeral P-256 key, relay session created, signed request fetched over the public host and checked (advertises the companion key, nonce bound to the address, client_id as pinned).
2. The builder scanned the QR with the wallet; consent screen shown (builder's report); presented.
3. 19:03:48 relay status `responded`. `pickup` decrypted locally: JWE alg ECDH-ES, enc A128GCM; vp_token[pid] is an SD-JWT with 4 disclosures, 3745 bytes. Second pickup with the token: HTTP 410.
4. Verifier log after the run: nonce occurrences 0, claim-name occurrences 0, one line "relay: stored the wallet's encrypted response unopened".
5. `prove` (first attempt) stopped in the companion precheck: "KB-JWT has no exp". Fixed in companion/src/statement.ts (exp optional, iat window kept). Companion tests 21 pass.
6. `prove` (second attempt) passed every statement check (vct, aud, nonce binding, age shape, issuer exp, freshness by iat) and stopped in circuits/tools/gen-prover.ts:86: `"alg":"ES256"` is not within the first 96 decoded bytes of the issuer header.

The builder also answered the verifier's landing-page QR (plain flow, verifier decrypts): the inspector showed vct urn:eudi:pid:de:1 with the three requested claims. That run is separate evidence that the wallet accepts this leaf and request; it is not the relay route.

## Observed shapes (FACT, read from the decrypted presentation on the Mac)

| Item | Observed | Circuit or companion expectation | Result |
|---|---|---|---|
| RP certificate and SAN | wallet accepted the registrar leaf whose SAN is DNS:localhost, over a trycloudflare host | no SAN enforcement under x509_hash (runbook section 5) | as expected |
| Encryption to client key | ECDH-ES, A128GCM to the companion's ephemeral P-256 key | blind relay requires it | pass |
| KB-JWT aud | x509_hash:VE3qp3vLVkU8JyVmXkjL7CSDVxVoTFdTv5fAEwmjKOI | pinned client_id (WP13) | match |
| KB-JWT claims | aud, iat, nonce, sd_hash (no exp) | companion required exp | fixed (exp optional) |
| KB-JWT nonce | equals hex(sha256(address20 || challenge32)) | address binding | match |
| vct | urn:eudi:pid:de:1 | urn:eudi:pid:de:1 | match |
| Issuer | https://preprod.pid-provider.bundesdruckerei.de; header alg ES256, typ dc+sd-jwt, kid (156 chars), x5c with one certificate (824 chars) | ES256, dc+sd-jwt, one x5c | match |
| Issuer header key order | x5c, kid, typ, alg; header 1033 decoded bytes; typ at byte 1001, alg at byte 1019 | alg and typ within the first 96 decoded bytes (HEADER_PREFIX_RAW, main.nr:33) | FAIL, circuit change needed (WP22) |
| Age claim shape | disclosure `age_equal_or_over` is an object with `_sd`; separate disclosure `18` with boolean true (nested selective disclosure) | one of three shapes (statement.ts:96-107) | accepted by the companion; not yet reached in the generator |
| Disclosed claims | family_name, given_name, age_equal_or_over, 18 | given_name, family_name, age_equal_or_over.18 | match |
| Status | status_list uri under the preprod PID provider, idx present | not checked on the Noir route (trust-boundaries.md gap 3) | recorded |
| Issuer exp, iat | exp 1790035200, iat 1788825600 | committed expiry is the issuer exp | ok |
| Sizes vs bounds | header b64 1378 of 2304, payload json 929 of 2304, signature b64 86, kb header b64 40 of 128, kb payload b64 282 of 384 | WP13 bounds | within bounds |

## Verdict

G0 pass criteria 1 to 6 of docs/spec-g0.md (official-wallet round trip through the relay, client-key decryption, nonce binding, no plaintext at the verifier, parsed claim shape recorded): PASS. The proof itself is not part of the G0 verdict; it is the next gate and is blocked by the header layout (WP22). Decision table branch: the wallet does encrypt to the client key, so the client-side route stays the main path; no SP1 fallback needed for this reason.

## Proof over the real presentation (2026-09-08, later the same evening)

After WP22 (circuit checks alg and typ in a movable 96-byte header window; VK_HASH 0x2e13794a77895882c11e891c641277cede611206c575ca2a35e8fe7724758a64, circuit_size 1,039,135), the companion proved the official-wallet presentation from the private run directory on main 7160255: statement holds natively, nargo execute, bb prove 4.41 s on the M3 Max, bb verify ok, proof 10,304 bytes, 86 public inputs. Public inputs (not personal data): over18 1, expiry 1790035200, issuer_key_hash 0xb4f2bfa1df99f06e588d39931b2cfd517a2befe8737c7f86bfa5c668d2abe079 (sha256 of the sandbox PID issuer's SEC1 key; this is the value to pin in NoirPidVerifier for the sandbox), nonce as in the session, subject the bound address. The proof was not submitted to a bridge or a chain in this run.

## Follow-ups

- WP22: done the same evening (merged 7160255); see the proof section above.
- WP23: the same optional KB exp in the SP1 host (check_kb_freshness) and the bridge (KB_JWT_WINDOW_SECS); confirm the SP1 guest's JSON parsing is key-order independent.
- Builder: add the wallet version string to this file; keep the private run directory out of every commit.
