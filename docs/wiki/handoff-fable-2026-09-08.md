---
type: handoff
title: Attestat, independent review and next Fable work
updated: 2026-09-08
owner: Fable lead
status: proposed
---

OPINION: Continue Attestat and finish one demonstrable route. The substantial local build changes the task from feasibility research to integration, policy correctness and submission. The final transcript recap that only builder tasks remain is too strong. Several implementation and documentation tasks remain with Fable.

This is a review and proposed work order, not a new product decision or a completed release gate. The review changed documentation only. Existing product decisions remain authoritative until the builder changes them.

FACT, review scope: four parallel reviews covered the complete 6,688-line [Fable transcript](/Users/bioharz/git/ethglobal/nachweis/2026-09-08-192458-we-did-a-lot-and-we-pivoted-many-times-check-sp.txt), current wiki, contracts and service, proof statements, device workflows, and current official submission sources. Inspected revisions: wiki `a8d5fff`, app `0fd7312`, verifier relay `8517398`, site `97dff23`. This was a bounded defensive review, not a full circuit audit. No phone presentation, new proof generation, external transaction, deployment or submission was performed.

FACT, executed during this review on the builder's Mac: the existing offline Foundry suite passed 87 tests, failed zero, and skipped one embedded fork test. Explicit fork test files were excluded. The suite includes actual Noir fixture verification; SP1 unit tests use a mock gateway. Other measurements below are recorded prior runs, not reruns by this review.

FACT, settled naming: **Attestat**, primary **attestat.dev**. The builder confirms purchases of .dev, .app, .xyz and .tech at Porkbun. The registration record and naming rationale live in [name-and-domains.md](/Users/bioharz/git/ethglobal/nachweis/wiki/name-and-domains.md:35). Repository, directory and protocol identifier renames remain undecided. Do not restore a nachweis.dev purchase task.

OPINION, scope: preserve the issuer-side product, real ZK, Uniswap integration and the visual pitch. Use the official iPhone test wallet with the laptop companion as the first client-proof route to validate. The recorded Pixel purchase decision retains that companion fallback. Native Android and iOS proving are optional extensions after the main route passes. [Recorded decision](/Users/bioharz/git/ethglobal/nachweis/wiki/decisions.md:50).

The next work is grouped by dependency and owner. These identifiers are review workstreams, not replacements for the existing WP numbers.

| Group | Owner | Work and acceptance | Stop condition |
|---|---|---|---|
| A. Eligibility and service boundaries | Fable implementation teammate, lead reviews the design | Enforce the intended combination of identity evidence, address binding and authorized issuer approval. Define the accepting party for freshness, issuer trust and status. Protect issuer operations and private result transfers. Unapproved evidence leaves both doors closed; approval opens the demonstrated actions; withdrawal closes them; reapproval requires issuer authority. | No public bridge or deploy-ready claim while these conditions are unresolved. |
| B. Official-wallet integration | Fable prepares, builder operates the wallet | Provide one reproducible G0 runbook and sanitized evidence capture. Establish actual RP identity, trust, audience, credential type, age shape and issuer key, then complete the official-wallet blind-relay plus companion flow with address proof enabled. | A failed official-wallet client-key round trip selects the documented SP1 fallback. An unsupported age shape requires a real implementation or a narrower claim. |
| C. Submission and evidence | Fable documentation teammate, builder submits | Repair status records and disclosure; prepare source publication, AI attribution, required development artifacts, Uniswap feedback and current form text. Every completed item has matching evidence and every submitted form has a receipt or recorded confirmation. | No local fork labelled live Sepolia; no fixture or simulator labelled an official-wallet physical-device run. |
| D. Attestat presentation and release preparation | Fable presentation teammate, lead integrates | Rename public copy, metadata, app labels, README and submission text; keep internal identifiers stable. Prepare HTTPS hosting on attestat.dev and redirects from .app/.xyz/.tech. The interactive explanation is labelled separately from the working application. | Publication follows the selected flow's acceptance evidence and the builder's existing publication authority. No repository move or extra domain purchase is inferred. |

Groups A, B preparation, C and D copy preparation can run independently. Final integration follows the wallet evidence and the policy decision. The lead retains the running context, resolves shared-file changes and keeps deployment actions on the main thread.

**The central product mismatch is confirmed in code.** FACT: [product.md](/Users/bioharz/git/ethglobal/nachweis/wiki/product.md:25) requires a separate issuer approval. The registry deliberately offers [permissionless proof attestation](/Users/bioharz/git/ethglobal/nachweis-app/contracts/src/AttestationRegistry.sol:112), and the bridge [submits a Noir proof immediately](/Users/bioharz/git/ethglobal/nachweis-app/service/src/api.rs:493). The UI explicitly retains [Approve as an alternative operator route](/Users/bioharz/git/ethglobal/nachweis-app/app/src/components/IssuerPending.tsx:45). These are two alternative routes today, not two required conditions.

FACT: the transcript contains no explicit builder decision replacing separate issuer approval with proof-only eligibility. It first [accepts their separation](/Users/bioharz/git/ethglobal/nachweis/2026-09-08-192458-we-did-a-lot-and-we-pivoted-many-times-check-sp.txt:2189), then records the automatic path as implementation work. OPINION: preserve the adopted product requirement and reconcile registry, bridge, UI and tests around it. Merely delaying the UI cannot establish the on-chain policy.

FACT: the issuer card claims [name matching](/Users/bioharz/git/ethglobal/nachweis-app/app/src/components/IssuerPending.tsx:71), while the bridge integration [returns public values without names](/Users/bioharz/git/ethglobal/nachweis-app/app/src/verifier.ts:261). OPINION: use an explicitly established demo customer record and state what the issuer actually approves. Do not describe a hidden name match. Identity evidence, customer-record association, address control and the issuer's authorization are distinct facts.

**Service preparation remains agent work.** FACT: the [bridge router](/Users/bioharz/git/ethglobal/nachweis-app/service/src/api.rs:66) has no HTTP authorization layer for its issuer operations; [operator attestation](/Users/bioharz/git/ethglobal/nachweis-app/service/src/api.rs:559) and [revocation](/Users/bioharz/git/ethglobal/nachweis-app/service/src/api.rs:596) exercise the configured operator authority. The verifier documentation already describes its presentation result transfer as [private infrastructure](/Users/bioharz/git/ethglobal/nachweis-verifier-relay/docs/bridge-mode.md:30). Investor address signatures do not authenticate the issuer.

FACT: the verifier's [result handler](/Users/bioharz/git/ethglobal/nachweis-verifier-relay/verifier-service/src/handlers.rs:1143) returns the full verified PID; its default also includes the raw presentation. Disabling the raw-presentation flag still leaves disclosed values in the serialized PID view. See [default configuration](/Users/bioharz/git/ethglobal/nachweis-verifier-relay/verifier-service/src/main.rs:71) and [revealed-claim serialization](/Users/bioharz/git/ethglobal/nachweis-verifier-relay/verifier-core/src/disclosure.rs:15). This does not satisfy WP0's written no-name-response criterion. OPINION: minimize public results, authenticate and isolate any plaintext transfer needed for SP1, protect issuer actions, and bound session/proving resource use before exposing the service. Verify these properties with synthetic-data regression checks.

**Proof guarantees need a route-specific contract.** FACT: credential expiry is committed by the proofs. The Noir bridge checks [subject, nonce, age and credential expiry](/Users/bioharz/git/ethglobal/nachweis-app/service/src/api.rs:470), but does not check session age or KB-JWT time there. The [handoff deadline](/Users/bioharz/git/ethglobal/nachweis-app/service/src/api.rs:251) controls fetching handoff data. The [companion's freshness check](/Users/bioharz/git/ethglobal/nachweis-app/companion/src/prove.ts:63) is client behavior. The claim that the bridge checks KB freshness for Noir is therefore unsupported. Trust-chain validation, accepted issuer-key pinning and status-list checking also need separate statements; the Noir bridge receives no credential or status evidence from which to perform those checks.

FACT: SP1 checks audience against a [private prover input](/Users/bioharz/git/ethglobal/nachweis-app/prover-sp1/lib/src/lib.rs:41) whose value is absent from the [committed public values](/Users/bioharz/git/ethglobal/nachweis-app/prover-sp1/lib/src/lib.rs:292). Noir instead has a [compiled audience constant](/Users/bioharz/git/ethglobal/nachweis-app/circuits/pid-sdjwt/src/constants.nr:65). OPINION: bind the intended relying party and define freshness and deployment scope at the accepting boundary, or explicitly narrow the claim. Do not call the routes identical or claim that all verification is independent of the operator. This review does not establish full JSON/circuit soundness.

FACT, useful existing protections: proof adapters bind the subject, issuer key, policy, predicate bits and expiry; the registry consumes nonces and prevents the proof path from reopening a revoked record. The [Uniswap checker](/Users/bioharz/git/ethglobal/nachweis-app/contracts/src/uniswap/EudiAllowlistChecker.sol:58) reads current registry eligibility. The [fund-token rule](/Users/bioharz/git/ethglobal/nachweis-app/contracts/src/FundToken.sol:60) checks recipients and has issuer/redemption exceptions. State the demonstrated subscription and pool refusals precisely; do not imply a universal freeze of all outgoing transfers.

The evidence for the existing routes has different boundaries. FACT: this table reports what the cited run records actually say.

| Recorded run | Established | Still unverified by that run |
|---|---|---|
| SP1 Groth16 pipeline | Synthetic presentation, real proof, real SP1 gateway on a local Sepolia fork, address signature, subscription and revoke. [Run record](/Users/bioharz/git/ethglobal/nachweis-app/docs/e2e-local.md:16) | Official wallet, live issuer trust, public-chain transaction and browser Groth16 path. |
| Noir companion handoff | Stub wallet, real Noir proof and HonkVerifier on Anvil, address proof enabled. [Run record](/Users/bioharz/git/ethglobal/nachweis-app/docs/two-device.md:58) | Actual official-wallet encrypted response. |
| Android emulator | Bundled vector, real proof accepted on Anvil; fixture issuer configuration and address-proof enforcement disabled for this phase. [Run record](/Users/bioharz/git/ethglobal/nachweis-app/docs/two-device.md:95) | Physical Pixel performance and complete investor address-binding flow on that device. |
| Browser app | Local development signer, Noir via companion and stub wallet; SP1 mock mode. [Run record](/Users/bioharz/git/ethglobal/nachweis-app/app/README.md:57) | Real extension interactions, official wallet, browser SP1 Groth16 and the complete live two-door scene. |
| iOS simulator | Bundled-vector proof and desktop verification. [Run record](/Users/bioharz/git/ethglobal/nachweis-app/prover-ios/README.md:103) | Physical iPhone proving and browser-bound handoff. The [current submit flow](/Users/bioharz/git/ethglobal/nachweis-app/prover-ios/NachweisProver/Model/FlowModel.swift:227) creates another session. |

OPINION, use these acceptance gates in order:

1. **Prepare G0.** Fable provides an absolute-path launcher/runbook for the actual relay worktree, a private evidence location, minimized public results, and an explicit pass/fail record. Resolve RP certificate/SAN compatibility before consuming a wallet presentation. Record sample wallet version, actual audience, credential type, nested age shape, bounds, expiry and validated issuer key. A plain G0 run proves the official-wallet/verifier round trip; it does not prove the client-proof or chain flow.
2. **Run the official wallet.** The builder uses the official test wallet on the iPhone. FACT: the documented birthdate fallback is not implemented throughout the current request/proof path; requests still select the [minimal age predicate](/Users/bioharz/git/ethglobal/nachweis-verifier-relay/verifier-service/src/handlers.rs:450), and the [companion precheck](/Users/bioharz/git/ethglobal/nachweis-app/companion/src/statement.ts:95) requires it. Check the actual response before deciding whether fallback work is necessary. A request for birthdate alone does not make the current circuit prove age from birthdate.
3. **Adapt to observed evidence.** Fable validates the issuer and payload shape and synchronizes circuit constants, verification keys, generated verifier and fixtures if they change. There is no captured sandbox PID recorded in [REALISM.md](/Users/bioharz/git/ethglobal/nachweis-app/circuits/pid-sdjwt/REALISM.md:54). Changing the RP certificate can change the audience and compiled artifacts. Never use a fixture issuer hash as live issuer evidence. Keep raw presentations, pickup credentials and witnesses out of commits and public logs.
4. **Complete the main route locally.** Use a browser session with real address signing, a fresh official-wallet presentation encrypted to the companion key, a real Noir proof, the enforced issuer decision and one registry. Demonstrate subscription and the Uniswap action, withdrawal, and both refusals. Keep address-proof enforcement enabled. Record where names were disclosed, who could decrypt, and which trust checks were performed. If client-key encryption fails, select SP1 with the stated server plaintext boundary; it still needs the actual-wallet flow and its own acceptance evidence.
5. **Repeat the selected route on Sepolia.** Fable prepares exact configuration, verified issuer/policy pins, reproducible deployment instructions and publication artifacts. The lead handles deployment under the builder's authorization. Record deployed addresses and actual receipts for proof acceptance and the demonstrated financial actions/refusals. Browser, proof and Uniswap fork tests do not substitute for this complete run.

**The wiki needs an evidence repair, not another architecture debate.** FACT: [WP9 site work](/Users/bioharz/git/ethglobal/nachweis/wiki/work-packages.md:38) reuses the unrelated contract-fix result. [WP11 Multipaz](/Users/bioharz/git/ethglobal/nachweis/wiki/work-packages.md:40) reuses SP1 integration results, while another WP11 row correctly names that integration. [WP5](/Users/bioharz/git/ethglobal/nachweis/wiki/work-packages.md:31) has a desktop title and a Pixel acceptance test. Several green rows retain unmet deployment/device criteria. OPINION: split local implementation, official-wallet, physical-device and deployment evidence explicitly while retaining the schema's status values. Give the duplicate packages distinct identities. Multipaz remains unverified and optional. Remove obsolete domain purchase, missing-endpoint and mobile-not-built reminders where the current sources supersede them.

**Submission facts were verified on 2026-09-08.** The official schedule places check-in 1 at 2026-09-08 05:59 Vienna, already past, and check-in 2 at 2026-09-11 05:59 Vienna. Whether the first was submitted is unverified. [Official schedule](https://ethglobal.com/events/ethonline2026). Final submission is **2026-09-13 16:00 UTC, 18:00 Vienna**. [Official start guide](https://ethglobal.com/events/ethonline2026/info/start).

FACT: Continuity requires clear prior-work disclosure and substantive event work; written notice delivery remains unconfirmed locally. [Rules](https://ethglobal.com/rules), [local open item](/Users/bioharz/git/ethglobal/nachweis/wiki/open-questions.md:42). The event also requires attribution of AI-assisted code/files/assets, meaningful participant involvement, and all specs, prompts and planning artifacts for a spec-driven workflow. Video constraints include 2 to 4 minutes, at least 720p, no speedups and no synthetic voice. [Submission guide](https://ethglobal.com/events/ethonline2026/info/details). OPINION: inventory the actual assistance and participant contribution accurately, prepare the required shareable development record, and review it for confidential data before publication.

FACT: [DISCLOSURE.md](/Users/bioharz/git/ethglobal/nachweis-app/DISCLOSURE.md:9) says the verifier was not modified during the event, although the relay branch is event work. The third-party circuit is still conditional there, and the event-work inventory omits implemented components. OPINION: distinguish pre-event base, event branch changes, adapted third-party code and newly authored code. Preserve source license notices. Prepare a reproducible source location for the relay branch and replace machine-specific dependency assumptions for judges.

FACT: Uniswap's applicable prize is **Best Uniswap Stack Contribution**, with a Continuity pool. It requires a public open-source repository, FEEDBACK.md, the separate Developer Feedback Form linking that file, and relevant README code references. The published review/audit wording does not require buying an external audit. [Prize brief](https://ethglobal.com/events/ethonline2026/prizes/uniswap-foundation). The app repository was still private in the read-only GitHub check. [FEEDBACK.md](/Users/bioharz/git/ethglobal/nachweis-app/FEEDBACK.md:63) labels fork evidence as live Sepolia experience. Publication and the feedback form remain outstanding in the records. Privy eligibility and an actual qualifying integration remain unconfirmed; keep it conditional. [Privy brief](https://ethglobal.com/events/ethonline2026/prizes/privy).

**Branding can be prepared independently.** FACT: the [site title](/Users/bioharz/git/ethglobal/nachweis-site/index.html:6), [footer domain](/Users/bioharz/git/ethglobal/nachweis-site/index.html:265), app README and product copy still use Nachweis. On 2026-09-08, public DNS reads showed all four Attestat domains on Porkbun nameservers; HTTPS probes from this Mac failed at the TLS handshake, so working hosting/redirects were not verified. The [public GitHub account endpoint](https://api.github.com/users/attestat) returned 404; that establishes no visible profile, not guaranteed registration availability.

OPINION: rename public surfaces to Attestat, use attestat.dev as the canonical URL, and prepare redirects for the three defensive domains. Retain repository paths, package identifiers, `nachweis:session:`, `nachweis.pid.over18.v1` and the handoff URI scheme for this submission unless a separate migration is justified and tested. Claiming the GitHub organization is useful if available; moving repositories and buying .eu/.de are optional builder decisions, not main-flow prerequisites.

OPINION: retain the site's colorful interactive explanation, but label its scripted sample sequence and link to the actual application. The [site script](/Users/bioharz/git/ethglobal/nachweis-site/app.js:32) simulates approval and chain actions. Reconcile absolute privacy, record-matching and exclusivity claims with the selected route. Public copy should promise no identity documents on chain and state the actual disclosure boundary; it should not promise that a linked address cannot identify someone. Keep regulatory claims qualified and primary-source checked before publishing them.

The builder-dependent items are wallet consent/interaction, confirmation of check-in and disclosure delivery, account/publication choices, testnet funding/signing, form submission and the human-voiced video. Fable should prepare every concrete artifact first and continue independent implementation while any of these inputs are pending. A private key should stay in the builder's local signer, not be pasted into chat.

OPINION, scope to park: a new product pivot, further naming searches, extra sponsor integrations, Multipaz, browser proving, optional native-phone parity, repository migration and additional domains. Native phone work can resume after the main route passes. The named-operator business test remains after the submission, as recorded; this prototype has no documented buyer validation yet.

Return from Fable: one concise report containing changed commits, selected route, exact acceptance evidence and its limitations, remaining blockers with owners, and the next single builder action. Update the wiki and submission copy to that same state. Do not label all work complete on the strength of fixture tests.
