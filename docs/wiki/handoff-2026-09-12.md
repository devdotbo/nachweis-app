---
type: handoff
title: Handoff 2026-09-12: the phone journey is on Sepolia, everything merged and pushed private, what is left, and the brief for the next independent review
updated: 2026-09-12
status: Written by the lead after the merges of 2026-09-12; final hashes in "State of the repositories"
sources:
  - nachweis-app/docs/evidence/sepolia-phone-2026-09-12.md (the journey with every hash re-verified)
  - wiki/review-readiness-2026-09-11.md (the review this day answered)
  - wiki/decisions.md "2026-09-12" (decisions of the day)
  - git in nachweis-app, nachweis-site, nachweis (2026-09-12 afternoon Vienna)
---

# Handoff 2026-09-12

Self-contained. Read this page, then the evidence record, then the files listed under "Brief for the independent review". Rules for the reviewer: English, absolute paths, no em dashes, say "unverified" instead of guessing, label FACT, CLAIM, OPINION. The deadline is Sunday 2026-09-13 18:00 Vienna.

## What happened today (FACT)

1. The 2026-09-11 readiness review was verified by four parallel agents. Every finding held: the swap classification defect (confirmed by reading, not affecting the recorded refusals), the phone leg never run on Sepolia, five wording items, the spec bundle missing raw/, disclosure delivery undocumented. Verdict of the lead: direction right, order wrong; run the phone take first and fix in parallel.
2. The official-wallet journey ran on Sepolia (chain 11155111) with a fresh investor 0xC616e007DC6FF4ac26d938F5FB9F062470EAbA44 that had no decision and nonce 0. An agent brought the tab to the QR through the Playwright extension; the builder scanned with the official German test wallet and clicked the rest. Record with every hash re-verified by public RPC: nachweis-app/docs/evidence/sepolia-phone-2026-09-12.md
   - attestWithProof 0x4ea1b1a4276ef2733c93cc87e4325485f5b80ca86f09fe9e8a96d58a73e8ed49, block 11687951, status 1, 4,582,644 gas, from the operator (the proof was made in the browser tab: witness 1.25 s, prover init 1.8 s, prove 11.0 s, verify 4.0 s on 16 threads; 40 s from pickup to attested).
   - Evidence-only state read at block 11687955 before approval: statusOf (true, false, false, 1790035200), isEligible false, both doors closed. This is the state the review asked for and no earlier record had.
   - approve 0x4566599623895f93985003758af6d4c20dc35351dbed1d1be51087e2aba9493d (11688109); subscribe 0x00c20b413f1b8b4be09d8fd9b0c462fefe99ed66e1b75c41377cfbb8c6332e1f (11688112); faucet mint, approval, Permit2, then swap 0xda990178ec3a1a972244ab280415eabac693944ec7a6efd81e5317eb29facd4b (11688117, 100 mUSD for 75.569267032175299294 NDF); revoke 0xd62cdf8d6c87b7e83c4e2a6e3078cd11747c67bd89e242851d644636df128af3 (11688133); refused swap 0x85e554b8ffc73f617c25275a636d216e7bd0c9adf36e4a307e8355e8d957d9f9 (11688138, status 0, replay decodes WrappedError(PermissionedHooks, beforeSwap, Unauthorized())).
   - Subscribe after the revoke was not sent: the page renders the button disabled. Final state statusOf (true, false, true, 1790035200), isEligible false.
3. Fixes, each reviewed before merge and all merged into local main:
   - Swap door classification (app): preflight errors are classified as reverted or unavailable; the mined receipt decides the outcome on every path; outcomes reverted, declined, not sent, unconfirmed are labelled; 8 unit tests (`bun test` in app/), the Playwright swap spec now asserts the receipt status on chain. Independent review: MERGE AFTER FIXES, fixes applied.
   - Wording (site, app, wiki): the registry accepts a proof or the operator key with approval as a separate step; the browser route keeps names in the tab; "Sepolia pending" and "no transaction was broadcast" removed; the shot list gate rewritten honestly; the beat 7 mUSD assumption corrected (a fresh investor mints first, four transactions).
   - Spec bundle (app docs/wiki): raw/ plans, reviews and memos are copied and classified in the manifest; CLAUDE.md included; transcripts excluded by name; a coverage script (`scripts/wiki-coverage.ts`) proves every source file is copied or listed with a reason; the pin is the wiki commit of the copy.
   - Alignment: counts (335 commits at 0f200f9, forge 155 passed 20 skipped, app unit 8, automation 19), dates, evidence links and the shot list figures agree across README, DISCLOSURE, ai-attribution, runbook, submission text, shot list, product and open questions. The Eligibility card reads "revoked" instead of "failed" after a revoke. The stack script labels the pool "Sepolia" in deployment mode.
   - Sanitization for a later public flip: no absolute home paths or the macOS username outside the sanitized bundle; Privy app id and signer id replaced by placeholders in the .env.example templates.
4. Secret scan before the push: all 590 distinct 64-hex values in HEAD and full history of the three repos were derived to addresses and compared with every known address; only anvil's public dev keys matched. No tokens, no PEM keys, no personal email. Commit author is the pseudonymous address. One exported transcript is tracked in the wiki repo's history (commit a8d5fff); the wiki stays private for that reason, and its sanitized copy in the app repo carries no transcript.
5. A second fresh investor for the video take, 0xD5a3AeD26885169fdC924EC1129b66353F8D33b8, was generated by the lead into the builder's gitignored .env (the key never appeared in the chat) and funded with 0.02 Sepolia ETH by the deployer (tx 0x1eeae27922d9c3ba4c825853e8d94b854e26101131576febf9b8b959ddc86d2f, block 11688187). The used phone investor is kept in .env under a dated name.

## State of the repositories (FACT, git after the push)

- nachweis-app main: see decisions.md "2026-09-12 evening" for the final hash; pushed to origin (private). Gates on that commit: forge offline 155 passed, 0 failed, 20 skipped; app `bun test` 8; automation 19; typecheck 0 errors; build clean (one pre-existing chunk-size warning); doc link check 0 broken; Playwright swap spec passed on a local pool stack.
- nachweis-site main: pushed (private). No build step.
- nachweis (wiki) main: pushed (private). The app's docs/wiki copy is pinned to the wiki commit named in nachweis-app/docs/wiki/README.md.
- GitHub: all three repositories PRIVATE. Visibility is the builder's flip.

## What is proven and what is not

Proven on Sepolia (FACT, records in docs/evidence/): deployment 2026-09-10; no-phone operator journey 2026-09-10; official-wallet journey 2026-09-12 with browser proof, evidence-only state, separate approval, subscribe, swap, revoke, refused swap.

Not established (FACT):
- The swap door's declined, not-sent and unconfirmed outcomes are covered by unit tests and types only; the local stack cannot decline or delay.
- The proof size on the card (10304 bytes, 86 public inputs) and the encoding on chain (13184-byte proof field, 4 registry public inputs) differ; the mapping is unverified.
- Contracts are not source-verified on Etherscan (no API key; deferred).
- attestat.dev is not hosted: the domain resolves to Porkbun parking addresses and the TLS handshake fails. The site links to it. GitHub Pages was the recorded recommendation (docs/hosting.md in the app repo).
- Check-in 2 (2026-09-11 05:59 Vienna) was optional by decision and there is no record it was sent.
- The video does not exist yet.

## Decisions of the day (FACT, decisions.md "2026-09-12")

Local merges approved ("green light, go"). Push private approved. Disclosure: the builder declined to name who gave the Classic "ok" and on which channel ("just be ok with that"); DISCLOSURE.md in the repo is the disclosure the submission carries; no separate notice drafted. The lead may generate and fund test investors without the builder ("you can always do that yourself"). Privy stays not claimed.

## Builder-only, in order

1. Video take: stack up with `scripts/browser-real-wallet-up.sh --deployment docs/deployments/sepolia-2026-09-10.md` (the lead brings it up on request; the investor in .env is the fresh 0xD5a3…33b8), one Chrome tab, recorder on, the click order of the shot list wiki/video-shotlist-spine.md beats 1 to 8. 2 to 4 minutes, 720p or better, own voice, no speedup. Then the lead writes the take's record from the run directory the same way as today.
2. Flip the three repositories public (the app repo must be public for the Uniswap prize and the spec-driven rule; the wiki may stay private because its sanitized copy is in the app repo; the site should be public if it is served from GitHub Pages).
3. Hosting: decide whether attestat.dev serves the site before the deadline (GitHub Pages plus a CNAME at Porkbun) or the submission links the repository page instead. The submission text has a bracketed slot for this.
4. Forms: ETHGlobal submission (text in wiki/submission-text.md, Sepolia variant with the 2026-09-12 hashes, Privy not claimed) and the Uniswap feedback form (FEEDBACK.md in the app repo). Keep the receipts.

## Brief for the independent review (GPT 6 Astra)

Question to answer: is this submission ready, true to its evidence, and pitchable; what would a judge find wrong in the first ten minutes; what is the strongest single claim and is it supported. Answer with FACT, CLAIM, OPINION labels and file references.

Read in this order:
1. This page.
2. nachweis-app/docs/evidence/sepolia-phone-2026-09-12.md, then re-verify at least the attestWithProof, the evidence-only state, the swap and the refused swap by public RPC yourself.
3. nachweis-app/README.md, DISCLOSURE.md, FEEDBACK.md, docs/ai-attribution.md, docs/wiki/README.md (the bundle manifest), docs/demo-runbook.md "Sepolia run".
4. wiki/submission-text.md and video-shotlist-spine.md: check every hash, count and claim against the records and the app strings.
5. The swap fix: nachweis-app/app/src/components/swap/useSwap.ts and calldata.ts, the unit tests next to them, app/e2e/swap.spec.ts. Judge whether the acceptance of your 2026-09-11 review is met.
6. The site at http://127.0.0.1:8787/ (served from nachweis-site main) against the code: every claim on the page must be supported.
7. Your own 2026-09-11 review: mark each of its findings as closed, open, or partly closed, with the file that closes it.

Do not: change files, send transactions, push, read .env, or re-decide the builder's decisions (Classic, Privy not claimed, disclosure as recorded). Report what remains for the builder and what you would still change in the texts, with exact wording.

## Known limits to carry into the submission wording (FACT)

Official German test wallet with the sandbox sample identity; sanctions and other checks simulated; the proof is made in the browser tab on the investor's computer (the wallet does not prove yet); revocation is manual; Privy standing order is a local showcase, not claimed; the dev signer in the page holds the keys (no wallet prompt); quote by slot0 and liquidity because the V4Quoter cannot quote a hooked pool that asks for msgSender.
