---
type: handoff
title: Handoff 2026-09-13 morning: the copy pass is merged, the video is prepared, what the builder does today in order
updated: 2026-09-13
status: Written by the lead overnight on the builder's mandate ("do all the steps that is needed to prepare everything"); final hashes in "State of the repositories"
sources:
  - wiki/decisions.md "2026-09-13 night" (every decision of the night, and what is explicitly not decided)
  - wiki/handoff-2026-09-12.md (the state this night started from)
  - [session transcript, not published] (the review round of 2026-09-12 evening: Codex, Grok, GLM, the lead)
  - git in nachweis-app, nachweis-site, nachweis (2026-09-13 about 01:30 Vienna)
---

# Handoff 2026-09-13 morning

Self-contained. Deadline today, 2026-09-13 18:00 Vienna. Video 2 to 4 minutes. Read this page top to bottom, then start at "Today, in order".

## What happened overnight (FACT)

Four Fable teammates ran one bounded task each; the lead reviewed every diff and merged locally. Nothing was pushed until the pushes listed under "State of the repositories". No transaction was sent, no key was read, no repository visibility changed, no form was submitted.

1. App (nachweis-app): the issuer caption "What Approve confirms" (on camera in beat 5) no longer says the issuer's verifier verified the presentation. It now describes the route that made the proof (browser tab on the demonstrated route; SP1 bridge, phone or companion on the others) and, while no proof is on record, the operator fallback. README: privacy wording per route, stale broadcast instructions replaced by the Sepolia evidence pointer, new sections "How a contract consumes a decision" (the Uniswap checker quoted as the integration example) and "Beyond the demo" (parked pages, labelled). docs/ai-attribution.md: the independent reviews (OpenAI GPT, xAI Grok, Z.ai GLM) are credited. The old seven-beat docs/video-shotlist.md is marked superseded by the wiki spine. Gates: typecheck 0 errors, build clean (pre-existing chunk warning), app unit tests 8 passed, doc links 158 checked 0 broken, bundle coverage ok. Forge not run (no Solidity changed). The dev server on port 62953 hot-reloaded the caption; it was not restarted.
2. Site (nachweis-site): registry acceptance semantics (evidence plus separate approval; operator fallback in one transaction without a proof), the browser-route privacy sentence, "Route 3, not built" with the verifier-adapter wording, Article 5f conditional ("Listed services must accept it from 2027", Article 5a date kept), the breach cards are now Revolut (11 Sep), IDScan.net (153 million, "as advertised"), Coinbase (kept). "KYC as a service" strikethrough became "Identity checks as a service". No CSS change; "153 million" wraps to two lines at desktop, accepted. No local paths in the public source.
3. Wiki (nachweis): submission-text.md carries the new tagline (85 characters), the incident sentence in the problem paragraph, the zkPassport sentence at the end of the honesty block, the verifier-adapter wording, and no fork fallback brackets any more (description now 525 words). product.md line 23 rewritten without "leaked" and without "the only way to hold less"; the customer due diligence caveat is stated. video-shotlist-spine.md: gate states Sepolia and the 2026-09-12 hashes, beat 4 ends with the verifier-adapter sentence (50 words spoken), beat 5 carries the new caption, beat 10's closing card ends with "The pool never learned about EUDI. It read one record." open-questions.md: the incident question is closed (site and description yes, video no), flagged for your confirmation. decisions.md "2026-09-13 night" records everything, including what is not decided.
4. Video preparation: see "The video" below.

## Decisions taken overnight, for your confirmation (OPINION of the lead, recorded as decided in decisions.md)

- The pitch stays the product sentence and the recorded journey. No SDK or toolkit framing. The tagline changes to: EUDI evidence, proven in zero knowledge, one on-chain decision any contract can read.
- The two incidents (Revolut, IDScan.net) are on the site and in the description, never in the video. Revolut's card shows the date, not a retention period. Attestat is not claimed to solve either case; a predicate is not customer due diligence.
- zkPassport: one sentence in the description, nothing in the video.
- Codex's five copy corrections of 2026-09-12 are applied (operator approval semantics, privacy per route, future wallet compatibility, Article 5f conditional, stale fallbacks removed).
- Retracted from the lead's own earlier verdicts: "any predicate the wallet can present" (the circuit fixes the German PID and age 18), the thesis in the first twenty seconds of the video (beat 4 already carries it), "the EU lacks ZK" (falsifiable; the claim is timing).

If you overrule any of these, say which; the texts are in three files: wiki/submission-text.md, nachweis-site/index.html, nachweis-app/README.md.

## Not decided, yours (FACT)

- Public flip of the repositories (recommended: app public, site public, wiki private).
- Hosting: attestat.dev is parked and its TLS handshake fails; the site's three GitHub links return 404 while the app repository is private. Recommended: the repository URL in the form, GitHub Pages later.
- Form submission (ETHGlobal and the Uniswap feedback form) and the receipts.
- The take.

## The video (FACT, files written overnight)

Script: wiki/video-script.md. Beats 0 to 8 and 10 (beat 9 omitted, Privy appears nowhere), 357 spoken words, 2:28 of speech at 145 words per minute inside 205 s of beat budget (3:25), so the take lands between 2:30 and 3:30 after the cuts listed in the script. Beat 4 keeps the ARF sentences and ends with the verifier-adapter sentence; beat 10 is 30 s (raised from 20 s, the shot list timings line says why). The script carries a Recording checklist and a Cut list.

Cards and teleprompter: video/ (README.md there). Serve with `python3 -m http.server 8790 --bind 127.0.0.1` from that directory, then open http://127.0.0.1:8790/cards.html (card 1 title and spoken line, card 2 the four limit lines, card 3 the product sentence and the closing line, card 4 a diagram of the demonstrated route as optional B-roll for the proof wait in beat 4; keys: space or right to advance, 1 to 4 to jump, f for full screen, ?card=N&still=1 for a static frame) and http://127.0.0.1:8790/teleprompter.html (one beat at a time with its budget, space starts and pauses the clocks, arrows move beats, r resets). Both pages are self-contained, no external resources, tested in headless Chrome and Playwright with no console errors; screenshots were checked by the lead (the diagram's labels match the shot list captions).

Not in the video, by decision: incidents, zkPassport, the gallery, the SDK word, any date. The video has no citations on screen and outlives the news cycle.

## Today, in order

1. Read "Decisions taken overnight" and answer with one word per item if any is overruled.
2. Record the take. Stack: the dev server on http://127.0.0.1:62953 is still the one from 2026-09-12 (pid 38034, cwd nachweis-app/app) and serves app main. Before recording, open the issuer console once and confirm the "What Approve confirms" text names the browser tab (it hot-reloaded; if it shows the old sentence, restart the stack with `scripts/browser-real-wallet-up.sh --deployment docs/deployments/sepolia-2026-09-10.md`). Investor 0xD5a3AeD26885169fdC924EC1129b66353F8D33b8 is funded and has no decision. Script, cards and teleprompter under "The video". Same clicks as 2026-09-12, beats 0 to 8 and 10, beat 9 omitted.
3. Tell the lead the take is done; the lead writes its evidence record from the run directory and fills the take commit into the shot list gate.
4. Flip the repositories (your call, see above). After the flip, re-check the site's GitHub links and the two new card sources once against the news of the day.
5. Paste the form from wiki/submission-text.md (Privy not claimed; the repository URL in the live-link slot unless the domain works). Submit the Uniswap feedback form from nachweis-app/FEEDBACK.md. Keep both receipts.

Cut order if the day compresses (OPINION): the form, the flip and the video are mandatory; the copy pass is already merged, so nothing else needs cutting. Do not add anything: no gallery, no zkPassport beat, no incident line in the video, no SDK word.

## State of the repositories (FACT, git after the pushes)

- nachweis-app main: merge 0ddd83c (wp45-copy d10b5fe plus the lead's caption fix for the no-proof case) and, on top of it, the bundle re-copy commit that pins docs/wiki to the wiki commit carrying this page (hash in `git log -1`). Gates on d10b5fe plus the caption fix: typecheck 0 errors, build clean, app unit tests 8 passed, doc links 0 broken, bundle coverage ok; forge not run (no Solidity changed). Pushed private.
- nachweis-site main a1b782e (merge c084546 of wp45-copy 30fdbdf, then the Article 5a date restored). Pushed private.
- nachweis (wiki) main: merge 14a0305 of wp45-copy 67987c2, then the commit that adds this page, the video script, the video/ directory and the beat 10 timing. Pushed private. Two exported transcripts of 2026-09-12 in wiki/ stay untracked on purpose (the wiki repository keeps transcripts out of commits).
- GitHub: all three repositories still PRIVATE.

## Known limits to carry into the submission wording (unchanged, FACT)

Official German test wallet with the sandbox sample identity; sanctions and other checks simulated; the proof is made in the browser tab on the investor's computer (the wallet does not prove yet); revocation is manual; Privy standing order is a local showcase, not claimed; the dev signer in the page holds the keys (no wallet prompt); quote by slot0 and liquidity because the V4Quoter cannot quote a hooked pool that asks for msgSender; the operator fallback stores and approves in one transaction without a proof; no x5c chain, no status list, no freshness window on the Noir route.
